export interface HouseType {
    id: number;
    title: string;
    description: string;
    location: string;
    latitude: string;
    longitude: string;
    recruitment: string;
    image_url: string;
    tags: string[];
}
